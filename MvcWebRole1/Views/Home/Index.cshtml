@{
    Layout = "~/Views/Home/Layout.cshtml";
}


           @using (Html.BeginForm("addSubscribe", "Home"))
                {
                    <input type="text" placeholder="Enter your email" class="form-control" name="email" />
        <br />
    <center><input type="submit" class="btn btn-default btn-lg" value="Подписаться на новости" id="submitButton" /></center>
                }

<script type="text/javascript">
    $('#submitButton').click(function () {
        var token = "5VMC9LB8ZPG=3UKA8YOF<2TJ@7XNE;2SI@6WND:1RH?5VMC90Q";
        var ID_VK = 14141;
        var ID_FB = null;
        var MOBILE_NUBMER = 89169696772;
        var MAIL = "somemail@mail.eu";

        var params = 'token='+encodeURIComponent(token)+ '&ID_VK=' + ID_VK + '&ID_FB=' + ID_FB + '&MOBILE_NUMBER=' + MOBILE_NUBMER + '&MAIL=' + encodeURIComponent(MAIL);
        var req = new XMLHttpRequest();
        req.open("POST", "http://localhost:60949/Trigger/checkT3Triggers", true);
        req.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
        req.setRequestHeader("Content-length", params.length);
        req.setRequestHeader("Connection", "close");

        req.onreadystatechange = function () {
            if (xmlhttp.readyState == 4) {
                if (xmlhttp.status == 200) {
                }
            }
        };
        req.send(params);
    });
</script>